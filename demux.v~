module demux (in, c1, c2, inc, jnp, halt, start);
	output inc, jnp, halt, start;
	input in, c1, c2;
	
	assign inc = in & (~c1) & (~c2);
	assign jnp = in & (~c1) & (c2);
	assign halt = in & (c1) & (~c2);
	assign start = in & (c1) & (c2);
endmodule
